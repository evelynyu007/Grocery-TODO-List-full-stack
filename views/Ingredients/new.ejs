<%- include('../partials/header') %>

<div>
  Before adding, please ensure that the Ingredient is not in the following list:
</div>
<section id="badge-container">
  <% ingredients.forEach(function(i) { %>

  <!-- Nutrition API ??? use form and POST-->
  <article class="badge">
    <%= i.name %>
    <!-- send the form-data to a file named  -->
    <!-- GET -->
    <!-- <a href="/ingredients/<%= i._id %>"><button>DETAILS</button></a> -->

    <form action="/ingredients/<%= i._id %>" method="POST">
      <input type="hidden" name="name" value="<%= i.name %>" />
      <input type="hidden" name="id" value="<%= i._id %>" />
      <button type="submit">DETAILS</button>
    </form>
  </article>

  <% }) %>
</section>

<!-- action = "/ingredients/ " -->
<form id="add-ingredient-form" action="/ingredients" method="POST">
  <label>Ingredient Name:</label>
  <input type="text" name="name" />
  <input type="submit" value="Add Ingredient" />
</form>

<%- include('../partials/footer') %>
